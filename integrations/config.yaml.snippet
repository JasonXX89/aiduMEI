# 两种接入方式 —— 选一个，别两个都开

aiduMEI v15 起支持两条接入 Hermes Agent 的路径。**推荐 A（插件）**，
B 只留给不方便装插件的场合（比如宿主 Hermes 不是自己管的）。

---

## A. 官方 MemoryProvider 插件（推荐）

拿到全部生命周期钩子：开局注入、逐轮归档、压缩前抢救、内置 memory 镜像、
三个工具（search / remember / status）、备份路径。

```bash
cp -r integrations/hermes-plugin/aidumem ~/.hermes/plugins/
hermes config set memory.provider aidumem
```

环境变量（可选，见仓库 `.env.example`）：

```bash
export AIDUMEM_URL=http://127.0.0.1:8767
export AIDUMEM_USER_ID=default
export AIDUMEM_DATA_DIR=~/aidumem      # 交给 Hermes 备份流程
```

验证：

```bash
hermes tools | grep aidumem            # 应看到 aidumem_search / remember / status
```

---

## B. Shell Hook（兜底方案）

没有压缩前抢救、没有工具、没有备份，但**读写两条线都有**。

### ⚠️ 两个脚本，缺一不可

| | 脚本 | 挂点 | 漏了会怎样 |
|---|---|---|---|
| **读线** | `aidumem-inject.sh` | `pre_llm_call` | 几分钟内就发现——模型明显不记事 |
| **写线** | `aidumem-ingest.sh` | `post_llm_call` | **一切看起来都正常**，几周后才发现新记忆一条没进 |

写线漏装是本项目出过的真实事故，而且是照着旧版本这份文档装出来的
（v21.2.0 之前这里只写了 `pre_llm_call`）。它之所以难发现，是因为
每一个绿灯都还是绿的：检索有结果（旧记忆还在）、`/health` 全绿
（库里的记忆确实健康）、集成自检通过（它自己调 `/add` 自己调 `/search`，
测的是被集成方不是集成本身）。唯一的症状是**新记忆再也没进来过**。

v21.2.0 起 `/health` 新增 `ingest_liveness_ok` 探针专门盯这件事：
有检索却零写入即判降级。

```bash
cp integrations/aidumem-inject.sh integrations/aidumem-ingest.sh ~/.hermes/agent-hooks/
chmod +x ~/.hermes/agent-hooks/aidumem-inject.sh ~/.hermes/agent-hooks/aidumem-ingest.sh
```

加到 `~/.hermes/config.yaml`：

```yaml
hooks:
  pre_llm_call:                                    # 读线：把记忆喂给模型
    - command: "~/.hermes/agent-hooks/aidumem-inject.sh"
      timeout: 8
  post_llm_call:                                   # 写线：把这一轮写回记忆库
    - command: "~/.hermes/agent-hooks/aidumem-ingest.sh"
      timeout: 10

hooks_auto_accept: true
```

装完**两个都要自检**，退出码 0 才算接上：

```bash
~/.hermes/agent-hooks/aidumem-inject.sh --selftest   # 读线
~/.hermes/agent-hooks/aidumem-ingest.sh --selftest   # 写线：真写一条再回读
```

真聊 5 轮之后，再确认写线活着：

```bash
python3 scripts/check_ingest_wiring.py               # 退出码非 0 即接线有问题
```

⚠️ 改过脚本内容后 Hermes 会因 mtime 校验要求重新批准 hook，这是设计如此。

细节读 `integrations/INTEGRATION_GUIDE.md`。
